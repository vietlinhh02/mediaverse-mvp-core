<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Weekly Mediaverse Digest</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; margin: 0; padding: 0; background-color: #f8f9fa; }
    .container { max-width: 600px; margin: 0 auto; background: white; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); }
    .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 40px 30px; text-align: center; }
    .header h1 { margin: 0; font-size: 28px; font-weight: 700; }
    .content { padding: 30px; }
    .greeting { font-size: 18px; margin-bottom: 30px; }
    .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 20px; margin: 30px 0; }
    .stat-card { background: #f8f9fa; padding: 20px; border-radius: 8px; text-align: center; }
    .stat-number { font-size: 24px; font-weight: bold; color: #667eea; margin-bottom: 5px; }
    .stat-label { font-size: 14px; color: #666; margin: 0; }
    .section { margin: 30px 0; }
    .section h2 { color: #333; font-size: 20px; margin-bottom: 15px; border-bottom: 2px solid #667eea; padding-bottom: 8px; }
    .content-item { background: #f8f9fa; padding: 15px; margin: 10px 0; border-radius: 8px; border-left: 4px solid #667eea; }
    .content-title { font-weight: 600; margin: 0 0 5px 0; }
    .content-meta { font-size: 14px; color: #666; margin: 0; }
    .follower-item { display: flex; align-items: center; background: #f8f9fa; padding: 15px; margin: 10px 0; border-radius: 8px; }
    .follower-avatar { width: 40px; height: 40px; background: #667eea; border-radius: 50%; margin-right: 15px; display: flex; align-items: center; justify-content: center; color: white; font-weight: bold; }
    .follower-info { flex: 1; }
    .follower-name { font-weight: 600; margin: 0; }
    .follower-date { font-size: 14px; color: #666; margin: 0; }
    .button { display: inline-block; background: #667eea; color: white; padding: 16px 32px; text-decoration: none; border-radius: 8px; font-weight: 600; margin: 20px 0; box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4); }
    .footer { background: #f8f9fa; padding: 30px; text-align: center; font-size: 14px; color: #666; border-top: 1px solid #e9ecef; }
    .unsubscribe { font-size: 12px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>📊 Your Weekly Digest</h1>
    </div>

    <div class="content">
      <p class="greeting">Hi {{userName}},</p>
      <p>Here's what happened in your Mediaverse community this week:</p>

      <div class="stats-grid">
        <div class="stat-card">
          <div class="stat-number">{{stats.views}}</div>
          <div class="stat-label">Content Views</div>
        </div>
        <div class="stat-card">
          <div class="stat-number">{{stats.likes}}</div>
          <div class="stat-label">New Likes</div>
        </div>
        <div class="stat-card">
          <div class="stat-number">{{stats.comments}}</div>
          <div class="stat-label">Comments</div>
        </div>
        <div class="stat-card">
          <div class="stat-number">{{stats.followers}}</div>
          <div class="stat-label">New Followers</div>
        </div>
      </div>

      {{#if topContent.length}}
      <div class="section">
        <h2>🔥 Top Performing Content</h2>
        {{#each topContent}}
        <div class="content-item">
          <p class="content-title">{{title}}</p>
          <p class="content-meta">{{views}} views • {{likes}} likes</p>
        </div>
        {{/each}}
      </div>
      {{/if}}

      {{#if newFollowers.length}}
      <div class="section">
        <h2>👥 New Followers</h2>
        {{#each newFollowers}}
        <div class="follower-item">
          <div class="follower-avatar">{{name.charAt 0}}</div>
          <div class="follower-info">
            <p class="follower-name">{{name}}</p>
            <p class="follower-date">Started following you</p>
          </div>
        </div>
        {{/each}}
      </div>
      {{/if}}

      <div style="text-align: center;">
        <a href="{{actionUrl}}" class="button">{{actionText}}</a>
      </div>

      <p>Keep up the great work! Your content is making an impact in our community.</p>

      {{#if trackingPixel}}{{{trackingPixel}}}{{/if}}
    </div>

    <div class="footer">
      <p>
        © {{year}} Mediaverse. All rights reserved.<br>
        <a href="{{preferencesUrl}}">Customize Digest Preferences</a>
      </p>
      <p class="unsubscribe">
        <a href="{{unsubscribeUrl}}">Unsubscribe from digest emails</a>
      </p>
    </div>
  </div>
</body>
</html>
